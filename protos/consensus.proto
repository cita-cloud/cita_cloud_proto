syntax = "proto3";

import "common.proto";

package consensus;

service ConsensusService {
    /// reconfigure consensus status
    rpc Reconfigure(common.ConsensusConfiguration) returns (common.Status);

    /// check block validity
    rpc CheckBlock(common.ProposalWithProof) returns (common.Status);
}
